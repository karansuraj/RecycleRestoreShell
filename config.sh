#!/bin/sh

# Establish vars for config dirs where recycle bin and restore file will be
# The path to the .restore.info file MUST exist
recycleDir=$HOME/recyclebin
restoreInfoPath=$recycleDir/.restore.info
